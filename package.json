{
  "name": "curiosita",
  "version": "3.0.0",
  "private": true,
  "description": "Curiosita — Plateforme educative statique",
  "scripts": {
    "build": "node scripts/build.js",
    "index": "node build-search-index.js",
    "serve": "python -m http.server 3001",
    "all": "node scripts/build.js && node build-search-index.js"
  },
  "devDependencies": {
    "clean-css": "^5.3.3",
    "terser": "^5.46.0"
  }
}
