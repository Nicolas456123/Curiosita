{
  "name": "curiosita",
  "version": "3.0.0",
  "private": true,
  "description": "Curiosita — Plateforme educative statique",
  "scripts": {
    "build": "node scripts/build.js",
    "index": "node scripts/build-search-index.js",
    "serve": "python -m http.server 3001",
    "cards": "node scripts/build-cards-db.js",
    "all": "node scripts/build.js && node scripts/build-search-index.js && node scripts/build-cards-db.js"
  },
  "devDependencies": {
    "cheerio": "^1.2.0",
    "clean-css": "^5.3.3",
    "terser": "^5.46.0"
  }
}
