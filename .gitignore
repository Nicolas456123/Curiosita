# Raw (non-minified) bundles — intermediate build artifacts
# Only the .min versions are referenced by HTML pages
curiosita.js
sr-app.js

# Temporary migration scripts
refactor_*.py

# Node
node_modules/

# OS
.DS_Store
Thumbs.db
